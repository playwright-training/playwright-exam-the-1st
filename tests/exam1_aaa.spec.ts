import { test, expect } from '@playwright/test';
import { LoginPage } from '../pages/LoginPage';
import { ReportPage } from '../pages/ReportPage';

test.describe('①AAA設計課題：レポートページ', () => {

  // ────────────────────────────────────────────
  // Arrange共通：ログインとレポートページへの遷移を
  // beforeEach にまとめてください
  // ────────────────────────────────────────────
  test.beforeEach(async ({ page }) => {
    // TODO: LoginPage でログインし、ReportPage に遷移する
  });

  // ────────────────────────────────────────────
  // 課題1-1: レポートテーブルの初期表示
  // ────────────────────────────────────────────
  test('全期間・全担当者でレポートテーブルに6件表示される', async ({ page }) => {
    // TODO: ReportPage を使って以下を確認する
    // ヒント: 初期状態は period=week なので、まず filterByPeriod('all') で全期間に切り替える
    // ヒント: getReportRowCount() でテーブルの行数を確認する
  });

  // ────────────────────────────────────────────
  // 課題1-2: 担当者フィルター
  // ────────────────────────────────────────────
  test('担当者「田中」で絞り込むとテーブルが2件になる', async ({ page }) => {
    // TODO: 全期間に切り替えた上で、担当者「田中」でフィルターする
    // テーブルの行数が2件であることを確認する
  });

  // ────────────────────────────────────────────
  // 課題1-3: 期間フィルター
  // ────────────────────────────────────────────
  test('期間「今週」で絞り込むとテーブルの件数が減る', async ({ page }) => {
    // TODO: まず全期間のテーブル行数を取得する
    // 期間を「今週」に切り替える
    // テーブルの行数が全期間より少ないことを確認する
  });

  // ────────────────────────────────────────────
  // 課題1-4: 集計カードの件数
  // ────────────────────────────────────────────
  test('集計カードの「総タスク数」がテーブルの行数と一致する', async ({ page }) => {
    // TODO: 全期間に切り替える
    // getStatValues() で集計カードの数値を取得する
    // 最初の値（総タスク数）がテーブル行数と一致することを確認する
  });

  // ────────────────────────────────────────────
  // 課題1-5: エクスポートボタン
  // ────────────────────────────────────────────
  test('エクスポートボタンをクリックするとトーストが表示される', async ({ page }) => {
    // TODO: exportReport() を呼び出す
    // トーストに「エクスポートしました（CSV）」と表示されることを確認する
    // ヒント: page.getByRole('status') でトースト要素を取得できる
  });

  // ────────────────────────────────────────────
  // 課題1-6: フィルターの組み合わせ
  // ────────────────────────────────────────────
  test('期間「今週」＋担当者「佐藤」で絞り込める', async ({ page }) => {
    // TODO: 期間を「今週」、担当者を「佐藤」に設定する
    // テーブルの行数が1件であることを確認する
    // ヒント: 今週（4/8〜4/14）で佐藤のタスクは「エラーハンドリングのテスト（4/12）」のみ
  });

});
